import VcButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
VcButtonGroup.install = function(Vue) {
  Vue.component(VcButtonGroup.name, VcButtonGroup);
};

export default VcButtonGroup;
