import VcCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
VcCheckboxButton.install = function(Vue) {
  Vue.component(VcCheckboxButton.name, VcCheckboxButton);
};

export default VcCheckboxButton;
